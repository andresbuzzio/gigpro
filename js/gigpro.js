// we initialise the owlCarousel

$(document).ready(function(){
  $(".owl-carousel").owlCarousel();
});

/* carousel on homepage auto rotates now

var owl = $('.owl-carousel');
owl.owlCarousel({
    loop:true,
    autoplay:true,
    autoplayTimeout:4000,
    autoplayHoverPause:true
});
* /
